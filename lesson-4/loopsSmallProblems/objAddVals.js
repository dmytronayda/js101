let ages = {
  Herman: 32,
  Lily: 30,
  Grandpa: 5843,
  Eddie: 10,
  Marilyn: 22,
  Spot: 237
};

// input object
// output number
//   initiate number with value 0;
//   make array with object values; // [['Herman', 32],..]
//   array for each loop
//     number += element;
//   return final value in number variable;

let number = 0;
// Object.values(ages).forEach(element => number += element);

console.log(number);